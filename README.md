# Grafics_1
```c++
glm::mat4 InitCameraTransform(const glm::vec3& Target, const glm::vec3& Up) // преобразования камеры
glm::vec3 Normalize(glm::vec3 v) // нормализация матрицы
glm::vec3 Cross(const glm::vec3 v1, const glm::vec3 v2) // Функция возвращает вектор, перпендикулярный плоскости
glm::mat4 InitPerspectiveProj(float fovy, float aspect, float znear, float zfar) // проецирование перспективы
```
